import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Angular-Ionic-ToDo-App',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
